# shadcn design system

This repository uses one UI system for every product. Agents load it automatically via `.cursor/rules/shadcn-design-system.mdc`.

Follow `design-system.md`. Tokens live in `app/globals.css` and `tailwind.config.ts`. Add primitives with `npx shadcn@latest add <name>`.
